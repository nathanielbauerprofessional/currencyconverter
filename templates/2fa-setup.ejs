<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= __("twofasetup") %></title>
</head>
<body>
  <h1><%= __("twofasetup") %></h1>
  <p><%= __("qrcode") %></p>
  <img src="<%= qrCodeData %>" alt="QR Code">

  <form method="POST" action="/2fa/setup">
    <label for="token"><%= __("entercodemessage") %></label>
    <input type="text" id="token" name="token" placeholder="Enter OTP" required>
    <button type="submit"><%= __("verify") %></button>
  </form>

  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>
</body>
</html>
